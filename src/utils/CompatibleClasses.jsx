export const CompatibleClasses = (courses, selected, unselectable) =>{
    
}
const meetSame = (courses, course) =>{
    const CourseStr = course.meets;
    const TwoParts = CourseStr.split(" ");
    const Start = Days[1].split("-");
    for (other in courses){
        if 
    }
}